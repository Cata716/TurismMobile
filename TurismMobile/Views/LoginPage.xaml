<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TurismMobile.Views.LoginPage"
             Shell.NavBarIsVisible="False"
             BackgroundColor="#F5F5F5">

    <ScrollView>
        <VerticalStackLayout Padding="30" Spacing="20" VerticalOptions="Center">

            <Frame BackgroundColor="#2196F3" CornerRadius="80" 
                   WidthRequest="160" HeightRequest="160" 
                   HasShadow="True" HorizontalOptions="Center"
                   Padding="0" Margin="0,0,0,30">
                <Label Text="🌍" FontSize="80" 
                       HorizontalOptions="Center" VerticalOptions="Center"/>
            </Frame>

            <Label Text="Bine ați venit!" 
                   FontSize="32" 
                   FontAttributes="Bold" 
                   HorizontalOptions="Center"
                   TextColor="#333333"/>

            <Label Text="Autentificați-vă pentru a continua" 
                   FontSize="16" 
                   HorizontalOptions="Center"
                   TextColor="#666666"
                   Margin="0,0,0,20"/>

            <Frame BackgroundColor="White" CornerRadius="10" 
                   HasShadow="True" Padding="0">
                <Entry x:Name="EmailEntry" 
                       Placeholder="Email" 
                       Keyboard="Email"
                       TextColor="#333333"
                       PlaceholderColor="#999999"
                       Margin="15,10"/>
            </Frame>

            <Frame BackgroundColor="White" CornerRadius="10" 
                   HasShadow="True" Padding="0">
                <Entry x:Name="PasswordEntry" 
                       Placeholder="Parolă" 
                       IsPassword="True"
                       TextColor="#333333"
                       PlaceholderColor="#999999"
                       Margin="15,10"/>
            </Frame>

            <Label x:Name="ErrorLabel" 
                   Text="" 
                   TextColor="Red" 
                   IsVisible="False"
                   HorizontalOptions="Center"
                   FontSize="14"/>

            <Button Text="AUTENTIFICARE" 
                    Clicked="OnLoginClicked"
                    BackgroundColor="#2196F3"
                    TextColor="White"
                    CornerRadius="10"
                    HeightRequest="50"
                    FontSize="16"
                    FontAttributes="Bold"
                    Margin="0,10,0,0"/>

            <Button Text="ÎNREGISTRARE" 
                    Clicked="OnRegisterClicked"
                    BackgroundColor="#4CAF50"
                    TextColor="White"
                    CornerRadius="10"
                    HeightRequest="50"
                    FontSize="16"
                    FontAttributes="Bold"/>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>